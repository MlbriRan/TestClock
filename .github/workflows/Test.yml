name: Test

on: push

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Information about workflow run and current job
        env:
          JOB_CONTEXT: ${{ toJson(job) }}
        run: |
          echo "************************************************************************************************"
          echo "$JOB_CONTEXT"
          echo "************************************************************************************************"

      - name: Some test commands
        run: |
          echo $PWD

      - name: Check job status
        run: |
          echo "Current job status is ${{ job.status }}."
