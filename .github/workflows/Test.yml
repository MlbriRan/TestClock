name: Test

on: 
  push:

jobs:
  TEST:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name:  Test cut command
        run: |
          echo "**************************"
          echo $GITHUB_REF
          echo "**************************"
          echo $GITHUB_REF | cut -d'/' -f3-
          echo "**************************"
          echo ${GITHUB_REF##*/}
          echo "**************************"
