name: Test

on: 
  push:

jobs:
  TEST:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name:  Test directory
        run: |
          echo "****************************************************"
          echo $PWD
          ls
          echo "****************************************************"
          cd ../../..
          echo $PWD
          mkdir -p .m2/repository
          echo "****************************************************"
          echo $HOME
          ls -lah $HOME
          echo "****************************************************"
